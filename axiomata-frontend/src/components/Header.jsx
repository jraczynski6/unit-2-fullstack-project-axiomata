export default function Header() {
  return (
    <header>
      <h2>Header Navigation</h2>
      {/* Add nav links, logout button, etc. */}
      {/* Add conditional rendering based on authentication */}
    </header>
  );
}